# Browser e2e suite — pinned to known-working versions, see CI for matrix.
pytest>=7.4
pytest-playwright>=0.4.4
playwright>=1.45,<2.0
pytest-xdist>=3.5
# filelock — gate the once-per-run baseline reset across xdist workers
# (canonical pytest-xdist "do it once" pattern in tests/_fixtures/server.py).
# NOT a transitive dep of pytest-xdist (it pulls execnet) — Rule 1: declare.
filelock>=3.12
httpx>=0.27
# TOTP MFA tests (TC-PA-MFA-*, TC-PA-STEPUP-*) — top-level import in test
# modules per Rule 7 ("read source before guessing"). Rule 1 forbids
# silent skip on missing dep — declared explicitly here.
pyotp>=2.9
pydantic-settings>=2.0
allure-pytest>=2.13
